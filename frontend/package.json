{
  "name": "crypto-quiz-frontend",
  "version": "1.0.0",
  "description": "前端应用 for Cryptography Knowledge Quiz System",
  "private": true,
  "proxy": "http://localhost:5000",
  "dependencies": {
    "@ant-design/icons": "^6.1.0",
    "antd": "^4.15.2",
    "axios": "^0.21.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-redux": "^7.2.3",
    "react-router-dom": "^5.2.0",
    "redux": "^4.0.5",
    "redux-thunk": "^2.3.0"
  },
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider --no-deprecation && set PORT=3001 && react-scripts start",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider --no-deprecation && react-scripts build",
    "test": "set NODE_OPTIONS=--openssl-legacy-provider --no-deprecation && react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "react-scripts": "4.0.3"
  }
}
